{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build active file",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "mkdir -p '${workspaceFolder}/build' && /usr/bin/g++ -std=c++17 -g -O0 -Wall -Wextra -Wno-sign-compare '${file}' -o '${workspaceFolder}/build/${fileBasenameNoExtension}'"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$gcc"],
      "detail": "Build C++ file into build folder"
    }
  ]
}
